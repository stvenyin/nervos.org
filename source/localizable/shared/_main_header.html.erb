<div class="header-nav clearfix">

  <div class="logo">
    <a href="/">
      <img src="<%= image_path(t_data.header.logo.src) %>" width="149" srcset="<%= parse_srcset_from_data(t_data.header.logo.srcset) %>"
           sizes="(max-width: 479px) 81vw, 149px">
    </a>
  </div>

  <div class="navbar">
    <% t_data.header.menu.each_with_index do |item, index| %>
      <a href="<%= item.last.link %>" class="nav-item <%= 'menu-active' if index == 0 %>"><%= item.last.name %></a>
    <% end %>
  </div>

</div>

